<UserControl x:Class="Trip.Views.PlanCabinetView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Trip.Views"
             xmlns:vm="clr-namespace:Trip.ViewModels"
             xmlns:models="clr-namespace:Trip.Models"
             mc:Ignorable="d" 
             Width="934" Height="450" Background="White">

    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/Styles/MainStyle.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>
    
    <Grid>
        <!--<Grid Visibility="{Binding NonePlansVisibility}">
            <Border>
                <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                    <TextBlock Text="등록 된 여행 계획이 없어요!" Style="{StaticResource HeaderTextStyle}"/>
                    <StackPanel HorizontalAlignment="Center" Margin="0 20">
                        <Button Style="{StaticResource MainBoardBtnStyle}">
                            <TextBlock Text="일정 등록하기" Style="{StaticResource LargeCenterTextStyle}" Margin="15 10"/>
                        </Button>
                    </StackPanel>
                </StackPanel>
            </Border>
        </Grid>-->
        <Grid Visibility="{Binding HasPlansVisibility}">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition/>
            </Grid.RowDefinitions>
            <StackPanel Grid.Row="0" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0">
                <Button Margin="5 10" Command="{Binding AddPlanCommand}" Style="{StaticResource NewPlanBtnStyle}">
                    <TextBlock Text="일정 추가하기" Style="{StaticResource NormalCenterTextStyle}" Margin="5"/>
                </Button>
                <Button Margin="5 10" Command="{Binding EditCommand}" MinWidth="70" Style="{StaticResource NewPlanBtnStyle}">
                    <TextBlock Text="{Binding EditText}" Style="{StaticResource NormalCenterTextStyle}" Margin="5"/>
                </Button>
            </StackPanel>
            <Border Grid.Row="1" BorderBrush="#D6C9B6" BorderThickness="0 2 0 0">
                <ScrollViewer VerticalScrollBarVisibility="Auto" CanContentScroll="False" Height="Auto">
                    <ItemsControl ItemsSource="{Binding Plans}" Padding="30 0 30 30">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate DataType="{x:Type vm:PlanDictonaryViewModel}">
                                <Border Style="{StaticResource MainBoardBorderStyle}" BorderBrush="#D6C9B6" BorderThickness="1" CornerRadius="6" Padding="10" Margin="0 7">
                                    <Grid Margin="0">
                                        <Grid.RowDefinitions>
                                            <RowDefinition Height="Auto"/>
                                            <RowDefinition/>
                                        </Grid.RowDefinitions>
                                        <Grid>
                                            <TextBlock Text="{Binding PlanName}" Style="{StaticResource NormalTextStyle}" VerticalAlignment="Center" HorizontalAlignment="Left" Height="20"/>
                                            <Button Style="{StaticResource ImageBtnStyle}" Visibility="{Binding EditVisibility}" Command="{Binding RemovePlanCommand}"
                                                    Width="20" Height="20" HorizontalAlignment="Right">
                                                <TextBlock Text="X" Foreground="Red" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                                            </Button>
                                            
                                        </Grid>
                                        <ItemsControl ItemsSource="{Binding Places}" Grid.Row="1">
                                            <ItemsControl.ItemTemplate>
                                                <DataTemplate DataType="{x:Type models:PlaceModel}">
                                                    <StackPanel Orientation="Horizontal" Margin="0 10 0 0">
                                                        <TextBlock Text="장소 이름" Style="{StaticResource NormalTextStyle}" Margin="10 0"/>
                                                        <TextBlock Text="{Binding NickName}" Style="{StaticResource NormalTextStyle}"/>
                                                        <TextBlock Text="주소" Style="{StaticResource NormalTextStyle}" Margin="10 0"/>
                                                        <TextBlock Text="{Binding PlaceName}" Style="{StaticResource NormalTextStyle}"/>
                                                        <TextBlock Text="머무는 시간" Style="{StaticResource NormalTextStyle}" Margin="10 0"/>
                                                        <TextBlock Text="{Binding StayHour}" Style="{StaticResource NormalTextStyle}"/>
                                                        <TextBlock Text="시간" Style="{StaticResource NormalTextStyle}" Margin="10 0"/>
                                                        <TextBlock Text="{Binding StayMinute}" Style="{StaticResource NormalTextStyle}"/>
                                                        <TextBlock Text="분" Style="{StaticResource NormalTextStyle}" Margin="10 0"/>
                                                    </StackPanel>
                                                </DataTemplate>
                                            </ItemsControl.ItemTemplate>
                                        </ItemsControl>
                                    </Grid>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </ScrollViewer>
            </Border>
        </Grid>
    </Grid>
</UserControl>
